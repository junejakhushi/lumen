# ShutterButton

The 72px snapshot button: an ivory disc inside an ivory ring.

- **Props:** `onCapture`, `label` (default “Take snapshot”), `disabled`, `state` (docs only).
- Hover and press shrink the disc; focus adds the gold ring outside.
