# SpecTable

The piece’s specification as a two-column table with thin gold rules and small-caps labels.

- **Props:** `rows` (`[{ label, value }]`; defaults to Code / Metal / Karat / Weight / Length / Width), `caption`.
- Order: Code, Metal, Karat, Weight, Length, Width. Values in Jost with tabular figures and metric units.
