# Select

A native select styled like `TextField`, with a line chevron.

- **Props:** `label`, `options` (strings or `{ value, label }`), `placeholder`, `hint`, `error`, `disabled`, plus native select props.
- Use for 5+ options or long labels (consultation location, language). For 2–4 short options use `SegmentedControl`.
