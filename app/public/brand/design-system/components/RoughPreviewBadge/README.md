# RoughPreviewBadge

A small dashed pill reading “Rough preview”, shown whenever the AR camera is live.

- **Props:** `children` (override text), `title` (tooltip; default explains that the finished piece is made by hand and will differ slightly).
- Always visible in the AR top bar. Works in both modes (it sits on `surface-control`).
