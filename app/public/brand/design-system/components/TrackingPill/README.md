# TrackingPill

The AR tracking status: “Finding you…”, “Looking good”, “Move a little closer”.

- **Props:** `status` `"finding" | "good" | "closer"`, `children` (override text).
- A polite live region. Each state has an icon as well as words (pulsing dots, a check, framing corners). The pulse stops under reduced motion.
